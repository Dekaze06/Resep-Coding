---
import Layout from "../../layouts/Layout.astro";
import GenerativeArtScene from "../../components/ui/GenerativeArtScene";
---

<Layout
    title="Studio Fullstack - Satusite AI Studio"
    description="Studio AI Agent khusus pengembangan aplikasi web lengkap dengan database in-memory, logika CRUD, manipulasi data, dan panel admin."
>
    <!-- Fluid Ambient Canvas Particles Background -->
    <div
        id="particles-bg"
        class="fixed top-0 left-0 w-full h-full overflow-hidden z-0 pointer-events-none"
    >
        <canvas
            id="particles-canvas"
            class="absolute inset-0 w-full h-full opacity-20"></canvas>
    </div>

    <!-- Subtle Ambient Purple & Fuchsia Gradients -->
    <div
        class="fixed top-[-10%] left-[20%] w-[500px] h-[400px] bg-purple-600/15 rounded-full blur-[140px] pointer-events-none z-0"
    >
    </div>
    <div
        class="fixed bottom-[-10%] right-[15%] w-[500px] h-[400px] bg-fuchsia-600/10 rounded-full blur-[140px] pointer-events-none z-0"
    >
    </div>

    <div class="relative z-10 flex flex-col min-h-screen bg-black text-white selection:bg-purple-500/30 selection:text-purple-200">
        <!-- SLEEK COMPACT STUDIO TOPBAR (HEADER HIDDEN) -->
        <header class="h-14 sm:h-16 px-4 sm:px-8 border-b border-zinc-800/60 bg-zinc-950/70 backdrop-blur-xl flex items-center justify-between z-20 shrink-0">
            <div class="flex items-center gap-3">
                <a
                    href="/studio"
                    class="px-2.5 py-1.5 rounded-lg bg-zinc-900/80 hover:bg-zinc-800 border border-zinc-800/90 hover:border-zinc-700 text-zinc-400 hover:text-white text-xs font-medium transition-all flex items-center gap-2 cursor-pointer group"
                    title="Kembali ke Pilihan Studio"
                >
                    <i class="fa-solid fa-arrow-left text-[11px] group-hover:-translate-x-0.5 transition-transform"></i>
                    <span>Pilih Studio</span>
                </a>
                <div class="h-4 w-[1px] bg-zinc-800"></div>
                <div class="flex items-center gap-2 select-none">
                    <img src="/logo.png" alt="Satusite Logo" class="w-5 h-5 object-contain" />
                    <span class="text-xs font-semibold text-zinc-300 tracking-tight">Studio Fullstack App</span>
                </div>
            </div>

            <!-- Right Status Indicator -->
            <div class="flex items-center gap-2">
                <span class="px-2.5 py-0.5 rounded-full text-[10px] font-mono font-semibold bg-zinc-900 text-zinc-300 border border-zinc-800">
                    FULLSTACK AI AGENT
                </span>
            </div>
        </header>

        <!-- HERO SECTION: DEDICATED FULLSTACK PROMPT STUDIO -->
        <section
            id="fullstack-prompt-studio"
            class="relative flex-1 flex flex-col justify-center py-12 sm:py-16 md:py-20 px-4 sm:px-6 text-center overflow-hidden"
        >
            <!-- 3D GENERATIVE MATTER SPHERE (Behind AI Chat Agent Box) -->
            <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[320px] h-[320px] sm:w-[480px] sm:h-[480px] md:w-[720px] md:h-[720px] pointer-events-none z-0 flex items-center justify-center overflow-hidden opacity-60">
                <GenerativeArtScene client:only="react" color="#a855f7" />
            </div>

            <div class="relative z-10 max-w-4xl mx-auto w-full space-y-4 sm:space-y-6">
                
                <!-- Title with Smooth Blur Fade Up Rotation -->
                <div class="space-y-2 flex items-center justify-center min-h-[44px] sm:min-h-[52px]">
                    <h1
                        id="fullstack-animated-title"
                        class="text-xl sm:text-2xl md:text-3xl font-normal tracking-tight text-zinc-100 font-sans leading-tight animate-title-in transition-all"
                    >
                        Studio Fullstack Web App
                    </h1>
                </div>

                <!-- PROMPT STUDIO BOX (FULLSTACK DEDICATED) -->
                <div
                    class="mt-4 sm:mt-6 max-w-3xl mx-auto bg-zinc-900/70 border border-zinc-800/90 rounded-2xl p-4 sm:p-5 text-left shadow-2xl backdrop-blur-xl"
                >
                    <form id="studio-prompt-form" class="space-y-3.5">
                        <div class="relative">
                            <textarea
                                id="studio-prompt-input"
                                rows="3"
                                placeholder="Tuliskan ide aplikasi atau fitur sistem..."
                                class="w-full bg-zinc-950 border border-zinc-800 focus:border-purple-500/60 rounded-xl p-3.5 sm:p-4 text-xs sm:text-sm text-zinc-100 placeholder-zinc-500 focus:outline-none transition-all resize-none font-sans"
                            ></textarea>
                        </div>

                        <!-- Prompt controls bar -->
                        <div class="flex items-center justify-between gap-3 pt-1">
                            <!-- Left: File Upload & Mode Indicator -->
                            <div class="flex items-center gap-1.5">
                                <label
                                    for="studio-file-upload"
                                    class="w-8 h-8 rounded-lg text-zinc-400 hover:text-white hover:bg-zinc-800 transition-colors flex items-center justify-center cursor-pointer"
                                    title="Upload Skema / File Data"
                                >
                                    <i class="fa-solid fa-paperclip text-xs"></i>
                                    <input
                                        id="studio-file-upload"
                                        type="file"
                                        class="hidden"
                                        accept=".md,.markdown,.txt,.json,.csv,.pdf,.doc,.docx,.html,.yaml,.yml,.sql,image/*"
                                    />
                                </label>
                                <span
                                    id="studio-file-indicator"
                                    class="hidden text-[11px] text-zinc-300 bg-zinc-800 px-2 py-0.5 rounded truncate max-w-[140px]"
                                ></span>

                                <!-- Static Fullstack Mode Badge with White Text -->
                                <div class="h-8 px-2.5 rounded-lg bg-zinc-900 border border-zinc-800 text-[11px] font-medium text-white flex items-center gap-1.5 shadow-sm">
                                    <i class="fa-solid fa-database text-xs text-zinc-300 shrink-0"></i>
                                    <span class="text-white font-medium">Fullstack & CRUD</span>
                                </div>
                            </div>

                            <!-- Right: Voice Icon & Generate Button -->
                            <div class="flex items-center gap-1.5">
                                <!-- Voice Dictation Icon -->
                                <button
                                    type="button"
                                    id="btn-studio-voice"
                                    class="w-8 h-8 rounded-lg text-zinc-400 hover:text-white hover:bg-zinc-800 transition-colors flex items-center justify-center cursor-pointer"
                                    title="Input Suara"
                                >
                                    <i
                                        id="icon-studio-voice"
                                        class="fa-solid fa-microphone text-xs"
                                    ></i>
                                </button>

                                <!-- Generate Button -->
                                <button
                                    type="submit"
                                    id="btn-studio-generate"
                                    class="w-8 h-8 rounded-lg bg-white hover:bg-zinc-200 text-zinc-950 flex items-center justify-center transition-all cursor-pointer shadow-md"
                                    title="Generate Fullstack"
                                >
                                    <i
                                        id="btn-studio-generate-icon"
                                        class="fa-solid fa-arrow-up text-xs text-zinc-950"
                                    ></i>
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </section>

        <!-- SUB-HEADER SECTION / FEATURES -->
        <section class="max-w-4xl mx-auto px-4 py-8 sm:py-12 border-t border-zinc-800/60">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 sm:gap-6">
                <div class="p-5 sm:p-6 rounded-2xl bg-[#0c0c0e]/80 border border-zinc-800/80">
                    <div class="w-9 h-9 rounded-xl bg-purple-950/60 border border-purple-800/40 text-purple-400 flex items-center justify-center mb-3">
                        <i class="fa-solid fa-database text-sm"></i>
                    </div>
                    <h3 class="text-sm font-bold text-white mb-1.5">In-Memory Database & LocalStorage</h3>
                    <p class="text-xs text-zinc-400 leading-relaxed">
                        Manajemen data multi-tabel relasional dengan sinkronisasi otomatis ke penyimpanan browser Anda.
                    </p>
                </div>

                <div class="p-5 sm:p-6 rounded-2xl bg-[#0c0c0e]/80 border border-zinc-800/80">
                    <div class="w-9 h-9 rounded-xl bg-purple-950/60 border border-purple-800/40 text-purple-400 flex items-center justify-center mb-3">
                        <i class="fa-solid fa-chart-line text-sm"></i>
                    </div>
                    <h3 class="text-sm font-bold text-white mb-1.5">4-Panel Switcher & Live Analytics</h3>
                    <p class="text-xs text-zinc-400 leading-relaxed">
                        Beralih cepat antara panel Kasir POS, Manajemen Master CRUD, Dashboard KPI, dan REST API Console.
                    </p>
                </div>
            </div>
        </section>

        <!-- DAFTAR PROYEK FULLSTACK YANG SUDAH DI-GENERATE (List2 Style Layout) -->
        <section id="fullstack-history-list-section" class="max-w-5xl mx-auto px-4 pb-20 pt-4">
            <div class="space-y-6">
                <!-- Header with Title and Search Filter at the Top -->
                <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 pb-4 border-b border-zinc-800/80">
                    <div class="flex items-center gap-2.5">
                        <span class="w-8 h-8 rounded-lg bg-purple-600/20 text-purple-400 border border-purple-500/30 flex items-center justify-center text-xs">
                            <i class="fa-solid fa-database"></i>
                        </span>
                        <h2 class="text-xl sm:text-2xl font-semibold text-white tracking-tight">Daftar Proyek Fullstack</h2>
                    </div>

                    <!-- Search Filter Input -->
                    <div class="relative w-full sm:w-72">
                        <i class="fa-solid fa-magnifying-glass absolute left-3 top-1/2 -translate-y-1/2 text-xs text-zinc-500"></i>
                        <input
                            type="text"
                            id="search-fullstack-history"
                            placeholder="Cari proyek fullstack..."
                            class="w-full pl-8 pr-3 py-2 rounded-xl bg-zinc-900/90 border border-zinc-800 text-xs text-white placeholder-zinc-500 focus:outline-none focus:border-purple-500/50 transition-colors shadow-inner"
                        />
                    </div>
                </div>

                <!-- History Rows Container with Separator layout -->
                <div id="fullstack-history-container" class="flex flex-col divide-y divide-zinc-800/80 border-y border-zinc-800/80">
                    <div class="py-12 text-center text-zinc-500 text-xs">
                        <i class="fa-solid fa-circle-notch fa-spin text-sm text-purple-400 mb-2"></i>
                        <p>Memuat daftar proyek fullstack...</p>
                    </div>
                </div>
            </div>
        </section>
    </div>

    <!-- Client Script -->
    <script is:inline>
        document.addEventListener("DOMContentLoaded", () => {
            const form = document.getElementById("studio-prompt-form");
            const input = document.getElementById("studio-prompt-input");
            const fileUpload = document.getElementById("studio-file-upload");
            const fileIndicator = document.getElementById("studio-file-indicator");
            const voiceBtn = document.getElementById("btn-studio-voice");
            const voiceIcon = document.getElementById("icon-studio-voice");
            const generateIcon = document.getElementById("btn-studio-generate-icon");

            let attachedFiles = [];
            let isGeneratingPrompt = false;
            let navTimer = null;

            function readFile(file) {
                return new Promise((resolve) => {
                    const isText = file.type.startsWith("text/") || 
                        file.name.endsWith(".md") || 
                        file.name.endsWith(".markdown") || 
                        file.name.endsWith(".txt") || 
                        file.name.endsWith(".json") || 
                        file.name.endsWith(".csv") || 
                        file.name.endsWith(".html") || 
                        file.name.endsWith(".yaml") || 
                        file.name.endsWith(".yml") || 
                        file.name.endsWith(".sql");
                    if (isText) {
                        const reader = new FileReader();
                        reader.onload = (ev) => resolve(ev.target.result || "");
                        reader.onerror = () => resolve("");
                        reader.readAsText(file);
                    } else {
                        resolve(`[Dokumen Terlampir: ${file.name}, Tipe: ${file.type || 'Dokumen'}, Ukuran: ${(file.size / 1024).toFixed(1)} KB]`);
                    }
                });
            }

            async function handleSelectedFile(file) {
                if (!file) return;
                const sizeKb = (file.size / 1024).toFixed(1) + " KB";
                const content = await readFile(file);
                attachedFiles = [{
                    name: file.name,
                    size: sizeKb,
                    type: file.type || "file",
                    content: content
                }];
                if (fileIndicator) {
                    fileIndicator.textContent = file.name;
                    fileIndicator.classList.remove("hidden");
                }
            }

            if (fileUpload) {
                fileUpload.addEventListener("change", async (e) => {
                    const file = e.target.files && e.target.files[0];
                    if (file) {
                        await handleSelectedFile(file);
                    } else {
                        attachedFiles = [];
                        if (fileIndicator) fileIndicator.classList.add("hidden");
                    }
                });
            }

            // Drag and drop support on form
            if (form) {
                form.addEventListener("dragover", (e) => {
                    e.preventDefault();
                    form.classList.add("border-purple-500/50");
                });
                form.addEventListener("dragleave", () => {
                    form.classList.remove("border-purple-500/50");
                });
                form.addEventListener("drop", async (e) => {
                    e.preventDefault();
                    form.classList.remove("border-purple-500/50");
                    const file = e.dataTransfer && e.dataTransfer.files && e.dataTransfer.files[0];
                    if (file) {
                        await handleSelectedFile(file);
                    }
                });
            }

            // Paste support on input
            if (input) {
                input.addEventListener("paste", async (e) => {
                    if (e.clipboardData.files && e.clipboardData.files.length > 0) {
                        e.preventDefault();
                        await handleSelectedFile(e.clipboardData.files[0]);
                    }
                });
            }

            // Voice Speech-to-Text Support
            if (voiceBtn && input) {
                const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
                if (SpeechRecognition) {
                    const recognition = new SpeechRecognition();
                    recognition.lang = "id-ID";
                    recognition.interimResults = false;

                    recognition.onstart = () => {
                        if (voiceIcon) voiceIcon.className = "fa-solid fa-circle-notch fa-spin text-xs text-purple-400";
                    };
                    recognition.onresult = (event) => {
                        const transcript = event.results[0][0].transcript;
                        input.value = input.value ? `${input.value} ${transcript}` : transcript;
                    };
                    recognition.onend = () => {
                        if (voiceIcon) voiceIcon.className = "fa-solid fa-microphone text-xs";
                    };
                    recognition.onerror = () => {
                        if (voiceIcon) voiceIcon.className = "fa-solid fa-microphone text-xs";
                    };

                    voiceBtn.addEventListener("click", () => {
                        try {
                            recognition.start();
                        } catch (e) {
                            try {
                                recognition.stop();
                            } catch (err) {}
                        }
                    });
                }
            }

            // --- FULLSTACK PROJECTS HISTORY FETCHER & RENDERER ---
            const fullstackHistoryContainer = document.getElementById("fullstack-history-container");
            const searchFullstackInput = document.getElementById("search-fullstack-history");
            let allFullstackProjects = [];

            async function loadFullstackHistory() {
                if (!fullstackHistoryContainer) return;
                let fetchedProjects = [];
                try {
                    const res = await fetch("/api/projects?mode=fullstack");
                    if (res.ok) {
                        const data = await res.json();
                        if (data && Array.isArray(data.projects)) {
                            fetchedProjects = data.projects;
                        }
                    }
                } catch (e) {
                    console.warn("Failed fetching fullstack history:", e);
                }

                // Merge fallback from localStorage
                try {
                    const storeRaw = localStorage.getItem("satusite_projects_store") || localStorage.getItem("emergent_projects_store");
                    if (storeRaw) {
                        const parsed = JSON.parse(storeRaw);
                        if (parsed && parsed.projects) {
                            const list = Object.values(parsed.projects).filter((p) => p.mode === "fullstack" || (!p.mode && !p.prd));
                            list.forEach(lp => {
                                if (!fetchedProjects.some(fp => fp.id === lp.id)) {
                                    fetchedProjects.unshift({
                                        id: lp.id,
                                        name: lp.name || "Aplikasi Fullstack",
                                        category: lp.category || "Fullstack App",
                                        mode: "fullstack",
                                        prompt: lp.prompt || (lp.messages && lp.messages[0]?.text) || "",
                                        code: lp.code || "",
                                        createdAt: lp.createdAt || lp.updatedAt || Date.now()
                                    });
                                }
                            });
                        }
                    }
                } catch (e) {}

                allFullstackProjects = fetchedProjects;
                renderFullstackHistoryList(allFullstackProjects);
            }

            function renderFullstackHistoryList(list) {
                if (!fullstackHistoryContainer) return;

                if (!list || list.length === 0) {
                    fullstackHistoryContainer.innerHTML = `
                        <div class="py-16 text-center text-zinc-500 text-xs">
                            <i class="fa-solid fa-database text-2xl text-zinc-600 mb-2 block"></i>
                            <p class="font-medium text-zinc-400">Belum ada proyek Fullstack yang dibuat</p>
                            <p class="text-[11px] text-zinc-600 mt-1">Tulis deskripsi ide sistem di atas untuk mulai membuat proyek Fullstack pertama Anda.</p>
                        </div>
                    `;
                    return;
                }

                fullstackHistoryContainer.innerHTML = list.map((item, idx) => {
                    const title = item.name || "Aplikasi Fullstack Web App";
                    const dateStr = item.createdAt ? new Date(item.createdAt).toLocaleDateString("id-ID", { day: "numeric", month: "short", year: "numeric" }) : "Tersimpan";
                    const promptText = item.prompt ? item.prompt.replace(/[*_#`]/g, "").trim() : "Aplikasi web mandiri dengan database in-memory, logika CRUD & panel manajemen.";

                    return `
                        <div data-row-full-idx="${idx}" class="grid items-center gap-4 px-3 sm:px-5 py-4 sm:py-5 grid-cols-1 md:grid-cols-4 hover:bg-zinc-900/40 transition-colors group cursor-pointer">
                            <!-- Col 1: Icon/Badge + Title + Meta -->
                            <div class="order-2 md:order-none flex items-center gap-3 min-w-0">
                                <span class="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl bg-zinc-900 border border-zinc-800 text-purple-400 shadow-sm group-hover:border-purple-500/30 group-hover:bg-purple-500/10 transition-all">
                                    <i class="fa-solid fa-database text-sm"></i>
                                </span>
                                <div class="flex flex-col gap-0.5 min-w-0">
                                    <h3 class="font-semibold text-white text-xs sm:text-sm truncate group-hover:text-purple-300 transition-colors">${title}</h3>
                                    <p class="text-[11px] text-zinc-500 flex items-center gap-1.5 flex-wrap">
                                        <span class="px-1.5 py-0.2 rounded text-[9px] font-mono font-bold bg-purple-500/10 text-purple-400 border border-purple-500/20">Fullstack</span>
                                        <span>•</span>
                                        <span>${dateStr}</span>
                                    </p>
                                </div>
                            </div>

                            <!-- Col 2: Description / Prompt (col-span-2) -->
                            <p class="order-1 md:order-none md:col-span-2 text-xs sm:text-sm font-normal text-zinc-300 line-clamp-2 leading-relaxed">
                                ${promptText}
                            </p>

                            <!-- Col 3: Actions -->
                            <div class="order-3 md:order-none flex items-center justify-end gap-1.5 ml-auto shrink-0" onclick="event.stopPropagation()">
                                <a
                                    href="/app?id=${item.id}"
                                    class="px-3 py-1.5 rounded-xl bg-zinc-800/80 hover:bg-zinc-700 text-zinc-200 hover:text-white border border-zinc-700/60 flex items-center gap-1.5 text-xs transition-colors"
                                    title="Buka di Studio Canvas"
                                >
                                    <span>Studio</span>
                                    <i class="fa-solid fa-arrow-up-right-from-square text-[10px]"></i>
                                </a>
                                <button
                                    type="button"
                                    data-download-full-idx="${idx}"
                                    class="w-8 h-8 rounded-xl bg-zinc-800/80 hover:bg-zinc-700 text-zinc-300 hover:text-white border border-zinc-700/60 flex items-center justify-center transition-colors cursor-pointer"
                                    title="Unduh HTML"
                                >
                                    <i class="fa-solid fa-download text-xs"></i>
                                </button>
                                <button
                                    type="button"
                                    data-delete-full-id="${item.id}"
                                    data-delete-full-title="${title.replace(/"/g, '&quot;')}"
                                    class="w-8 h-8 rounded-xl bg-zinc-800/80 hover:bg-red-500/20 text-zinc-400 hover:text-red-400 border border-zinc-700/60 flex items-center justify-center transition-colors cursor-pointer"
                                    title="Hapus Proyek"
                                >
                                    <i class="fa-solid fa-trash-can text-xs"></i>
                                </button>
                            </div>
                        </div>
                    `;
                }).join("");

                // Row click handler to open studio
                document.querySelectorAll("[data-row-full-idx]").forEach(row => {
                    row.addEventListener("click", () => {
                        const idx = parseInt(row.getAttribute("data-row-full-idx") || "0", 10);
                        const item = allFullstackProjects[idx];
                        if (item && item.id) {
                            window.location.href = `/app?id=${item.id}`;
                        }
                    });
                });

                // Download handler
                document.querySelectorAll("[data-download-full-idx]").forEach(btn => {
                    btn.addEventListener("click", (e) => {
                        e.stopPropagation();
                        const idx = parseInt(btn.getAttribute("data-download-full-idx") || "0", 10);
                        const item = allFullstackProjects[idx];
                        if (!item) return;
                        const code = item.code || `<!DOCTYPE html><html><head><title>${item.name}</title></head><body><h1>${item.name}</h1></body></html>`;
                        const blob = new Blob([code], { type: "text/html;charset=utf-8;" });
                        const url = URL.createObjectURL(blob);
                        const a = document.createElement("a");
                        a.href = url;
                        a.download = `${(item.name || "fullstack-project").toLowerCase().replace(/[^a-z0-9]/g, "-")}.html`;
                        a.click();
                        URL.revokeObjectURL(url);
                    });
                });

                // Delete handler
                document.querySelectorAll("[data-delete-full-id]").forEach(btn => {
                    btn.addEventListener("click", async (e) => {
                        e.stopPropagation();
                        const id = btn.getAttribute("data-delete-full-id");
                        const title = btn.getAttribute("data-delete-full-title") || "Proyek ini";
                        if (!id) return;
                        if (!confirm(`Hapus proyek "${title}" secara permanen?`)) return;

                        try {
                            await fetch(`/api/projects/${id}`, { method: "DELETE" });
                        } catch (err) {}

                        try {
                            const storeRaw = localStorage.getItem("satusite_projects_store") || localStorage.getItem("emergent_projects_store");
                            if (storeRaw) {
                                const store = JSON.parse(storeRaw);
                                if (store && store.projects && store.projects[id]) {
                                    delete store.projects[id];
                                    localStorage.setItem("satusite_projects_store", JSON.stringify(store));
                                }
                            }
                        } catch (err) {}

                        allFullstackProjects = allFullstackProjects.filter(p => p.id !== id);
                        renderFullstackHistoryList(allFullstackProjects);
                    });
                });
            }

            // Realtime search filter
            if (searchFullstackInput) {
                searchFullstackInput.addEventListener("input", (e) => {
                    const q = (e.target.value || "").toLowerCase().trim();
                    if (!q) {
                        renderFullstackHistoryList(allFullstackProjects);
                        return;
                    }
                    const filtered = allFullstackProjects.filter(p => {
                        const name = (p.name || "").toLowerCase();
                        const prompt = (p.prompt || "").toLowerCase();
                        return name.includes(q) || prompt.includes(q);
                    });
                    renderFullstackHistoryList(filtered);
                });
            }

            loadFullstackHistory();

            // Smooth Blur Fade Up Rotating Titles
            const titles = [
                "Studio Fullstack Web App",
                "Logika CRUD & Database In-Memory",
                "Panel Admin & Manajemen Data",
                "Alur Sistem & Operasi Backend"
            ];
            let currentTitleIdx = 0;
            const titleEl = document.getElementById("fullstack-animated-title");
            if (titleEl) {
                setInterval(() => {
                    titleEl.classList.remove("animate-title-in");
                    titleEl.classList.add("animate-title-out");
                    setTimeout(() => {
                        currentTitleIdx = (currentTitleIdx + 1) % titles.length;
                        titleEl.textContent = titles[currentTitleIdx];
                        titleEl.classList.remove("animate-title-out");
                        titleEl.classList.add("animate-title-in");
                    }, 400);
                }, 3500);
            }

            // Form Submission
            if (form && input) {
                form.addEventListener("submit", (e) => {
                    e.preventDefault();
                    if (isGeneratingPrompt) {
                        if (navTimer) clearTimeout(navTimer);
                        isGeneratingPrompt = false;
                        if (generateIcon) generateIcon.className = "fa-solid fa-arrow-up text-xs";
                        return;
                    }

                    let promptText = input.value.trim();
                    if (!promptText && attachedFiles.length > 0) {
                        promptText = `Bangun aplikasi fullstack lengkap dengan CRUD dan database in-memory berdasarkan dokumen lampiran ${attachedFiles[0].name}.`;
                    }
                    if (attachedFiles.length > 0) {
                        promptText = `[Lampiran: ${attachedFiles[0].name}] ` + promptText;
                        try {
                            sessionStorage.setItem("satusite_pending_attached_docs", JSON.stringify(attachedFiles));
                        } catch (err) {
                            console.warn("Storage save error:", err);
                        }
                    }
                    if (!promptText) return;

                    isGeneratingPrompt = true;
                    if (generateIcon) generateIcon.className = "fa-solid fa-square text-[10px] text-white";

                    navTimer = setTimeout(() => {
                        window.location.href = `/app?prompt=${encodeURIComponent(promptText)}&mode=fullstack`;
                    }, 400);
                });
            }
        });

        // Background Particles
        (function () {
            const canvas = document.getElementById("particles-canvas");
            if (!canvas) return;
            const ctx = canvas.getContext("2d");
            if (!ctx) return;

            let particles = [];
            const count = 45;

            function resize() {
                canvas.width = window.innerWidth;
                canvas.height = window.innerHeight;
            }

            function init() {
                resize();
                particles = Array.from({ length: count }, () => ({
                    x: Math.random() * canvas.width,
                    y: Math.random() * canvas.height,
                    vx: (Math.random() - 0.5) * 0.35,
                    vy: (Math.random() - 0.5) * 0.35,
                    size: Math.random() * 1.5 + 0.5,
                    opacity: Math.random() * 0.3 + 0.1,
                }));
            }

            function loop() {
                ctx.clearRect(0, 0, canvas.width, canvas.height);

                for (let i = 0; i < particles.length; i++) {
                    const p = particles[i];
                    p.x += p.vx;
                    p.y += p.vy;

                    if (p.x < 0) p.x = canvas.width;
                    if (p.x > canvas.width) p.x = 0;
                    if (p.y < 0) p.y = canvas.height;
                    if (p.y > canvas.height) p.y = 0;

                    ctx.fillStyle = `rgba(168, 85, 247, ${p.opacity * 0.35})`;
                    ctx.beginPath();
                    ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
                    ctx.fill();
                }

                requestAnimationFrame(loop);
            }

            window.addEventListener("resize", resize);
            document.addEventListener("DOMContentLoaded", () => {
                init();
                loop();
            });
        })();
    </script>

    <style>
        @keyframes titleFadeInUp {
            0% {
                opacity: 0;
                transform: translateY(12px);
                filter: blur(6px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
                filter: blur(0);
            }
        }
        @keyframes titleFadeOutUp {
            0% {
                opacity: 1;
                transform: translateY(0);
                filter: blur(0);
            }
            100% {
                opacity: 0;
                transform: translateY(-12px);
                filter: blur(6px);
            }
        }
        .animate-title-in {
            animation: titleFadeInUp 0.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }
        .animate-title-out {
            animation: titleFadeOutUp 0.35s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }
    </style>
</Layout>
